/*
 * To change this template, choose Tools | Templates
 * and open the template in the editor.
 */

package fr.utc.grid;

import fr.utc.data.Game;
import fr.utc.data.Player;
import fr.utc.data.Profile;
import fr.utc.exceptions.PositionConstructorLineException;
import fr.utc.grid.board.GridController;
import java.awt.BorderLayout;
import javax.swing.JFrame;


/**
 *
 * @author lo23a015
 */
public class MainTestBoard {

    /**
     * @param args the command line arguments
     */
   // public static void main(String[] args) throws PositionConstructorLineException
   // {
        /*GameFrame window = new GameFrame();
        window.loadView(new LogView(window));
        //new ResponseRequestPopUp("test");*/
/*
        Profile p = new Profile("Stringer Bell", "ssup");
        Profile p1 = new Profile("Omar Little", "ssup");
        GridController piw = new GridController(new Game(new Player(p, false, 2500), new Player(p1, true, 2500)),false);
        JFrame frame = new JFrame();
        frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        frame.setSize(1000,1000);
        frame.setLayout(new BorderLayout());
        frame.add(piw, BorderLayout.CENTER);
        frame.pack();
        frame.setVisible(true);*/
    //  }

}
